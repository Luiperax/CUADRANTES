# Dependencias del Generador de Cuadrantes de Seguridad Privada.
# Instalación:  pip install -r requirements.txt

ortools>=9.8          # Motor de optimización CP-SAT (CSP, PL, multiobjetivo).
openpyxl>=3.1         # Exportación a Excel con formato NATURGY.
reportlab>=4.0        # Exportación a PDF (cuadrante e informes).
PySide6>=6.6          # Interfaz gráfica en modo oscuro (Qt for Python).
APScheduler>=3.10     # Programador de la ejecución automática del día 15.
