# Dependencias para el DESPLIEGUE de la versión web en un servidor.
# No incluye PySide6 (interfaz de escritorio), que no es necesaria en el servidor.
# Instalación:  pip install -r requirements-web.txt

ortools>=9.8              # Motor de optimización CP-SAT.
openpyxl>=3.1             # Exportación a Excel con formato NATURGY.
reportlab>=4.0            # Exportación a PDF (cuadrante e informes).
fastapi>=0.110           # Servidor web.
uvicorn[standard]>=0.27  # Servidor ASGI.
jinja2>=3.1              # Plantillas HTML.
python-multipart>=0.0.9  # Formularios web.
